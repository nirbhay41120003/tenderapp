{
  "name": "tender-manager",
  "version": "1.0.0",
  "description": "Tender Manager PWA — single-file contractor app",
  "scripts": {
    "prepare-www": "node scripts/prepare.js",
    "build": "npm run prepare-www",
    "serve": "npx http-server www -p 8080",
    "start": "npm run prepare-www && npm run serve"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  }
}
